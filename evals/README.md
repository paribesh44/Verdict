# Evaluation Harness

This directory contains a GAIA 2.0 / ARE-style adapter runner.

## Run

```bash
python gaia2_are_runner.py --tasks tasks/sample_gaia2_tasks.jsonl --orchestrator-url http://localhost:8001
```

The current scaffold validates end-to-end trajectory execution and protocol output presence.
